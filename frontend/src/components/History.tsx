import { Container } from 'react-bootstrap';
import styles from '../styles/History.module.css';

function History() {
  return (
    <Container className={styles.history}>
      {/* ajouter contenu historique */}
      <div>History</div>
    </Container>
  );
}

export default History;
